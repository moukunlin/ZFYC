<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="cn.com.zfyc.dao.ShopDao">

    <insert id="save" >
        INSERT INTO
          shop
          (
            shop_name,
            type_id,
            name,
            user_id,
            address,
            lng,
            lat,
            area,
            shop_photo,
            in_store,
            license_photo,
            permit_photo,
            id_num,
            id_photo,
            bank_address,
            bank_nane,
            bank_name,
            bank_id,
            bank_phone,
            status,
            create_date

          )
          VALUES
	      (
            #{shop.shopName},
            #{shop.typeId},
            #{shop.name},
            #{shop.userId}
            #{shop.area},
            #{shop.address},
            #{shop.lng},
            #{shop.lat}
            #{shop.shopPhoto},
            #{shop.inStore},
            #{shop.licensePhoto},
            #{shop.permitPhoto},
            #{shop.idNum},
            #{shop.IdPhoto},
            #{shop.bankAddress},
            #{shop.bankNnme},
            #{shop.bankName},
            #{shop.bankId},
            #{shop.bankPhone},
            0,
            now()
            )

    </insert>

    <select id="checkPhoneNum" parameterType="String" resultType="cn.com.zfyc.bean.ShopEntity">
        SELECT * FROM  shop WHERE
        <if test="shopId != null">
            shop_id = #{shopId}
        </if>
    </select>

   <select id="findShopByUserId" parameterType="String" resultType="cn.com.zfyc.bean.ShopEntity">
      SELECT * FROM  shop where user_id = #{userId};
   </select>


</mapper>